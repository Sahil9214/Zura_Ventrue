import React from "react";

const ProjectsPage = ({ isOpen, onOpen, onClose, projects }) => {
  const handleCreateProject = () => {
    onOpen();
  };

  return <div></div>;
};

export default ProjectsPage;
